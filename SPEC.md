# GPU 예약 시스템 요구사항 명세서 (SPEC)

## 1. 목표
연구실 GPU 서버 3대(GPU 12장)를 여러 사용자가 겹치지 않게 예약하고,
2주 단위로 예약 현황을 시간대별로 시각화하는 웹 서비스.
(화면의 이전/다음 버튼으로 2주 뒤, 4주 뒤 기간도 볼 수 있다)
PC(유선 내부망)와 휴대폰(공용 와이파이) 브라우저에서 모두 접속 가능해야 한다.

## 2. 운영 환경
- 호스팅 컴퓨터: 개인 데스크탑, Ubuntu 24.04, 학교(KAIST) 내부망
- 접속 주소: `http://<데스크탑 IP>:9080` (포트 하나로 운영)
  - 8000번대는 이 컴퓨터의 강화학습·VLA 추론 서버와 충돌할 수 있어 피한다. (개발용 백엔드는 9081)
- 모든 시간은 한국 시간(Asia/Seoul) 기준으로 저장·표시한다.
- 터미널 없이 백그라운드로 실행되고, 재부팅 시 자동 시작되어야 한다 (systemd).

## 3. 기술 스택
- 백엔드: Python 3 + FastAPI, 가상환경(venv) 사용
- DB: SQLite (파일 하나)
- 프론트엔드: Vue 3 + Vite
- 배포 형태: Vue를 빌드한 정적 파일을 FastAPI가 함께 서빙 (프로세스 1개, 포트 1개)
- 유료 라이브러리는 사용하지 않는다. (FullCalendar 유료 플러그인 등 금지)
- 설정값(예약 규칙, 가입 코드, GPU 표시 이름 등)은 코드에 박지 말고 설정 파일로 분리한다.

## 4. GPU 구성
| 서버 | GPU 번호 | 모델 | 분류 |
|---|---|---|---|
| 서버 1 | 0, 1 | A6000 | 단주기 |
| 서버 1 | 2, 3 | A6000 | 장주기 |
| 서버 2 | 0, 1 | RTX 3090 | 단주기 |
| 서버 2 | 2, 3 | RTX 3090 | 장주기 |
| 서버 3 | 0 | A100 | 단주기 |
| 서버 3 | 1 | A100 | 장주기 |
| 서버 3 | 2 | maxQ 6000 | 단주기 |
| 서버 3 | 3 | maxQ 6000 | 장주기 |

- 단주기 GPU 6장, 장주기 GPU 6장.
- 서버 3의 A100/maxQ 번호는 반대일 수 있으므로, GPU 모델 표시 이름은 설정 파일에서 쉽게 바꿀 수 있어야 한다.
- GPU 목록은 앱 최초 실행 시 설정 파일을 기준으로 DB에 등록한다.

## 5. 예약 규칙
- 예약은 "시작 날짜·시간 ~ 종료 날짜·시간"으로 신청한다.
- 시간 단위: 1시간 (정시 기준). (설정값)
- 종료 시각은 시작 시각보다 뒤여야 한다.
- 예약 시작 시각은 이미 지난 시각일 수 없다.
  (기준은 '현재 시각이 속한 정시'. 14:20이면 14:00 시작은 허용, 13:00은 거부)
- **예약 길이 제한과 '며칠 이내 시작' 제한은 두지 않는다.**
  단주기 최대 시간, 장주기 최소 시간, 최대 예약 길이, 14일 이내 시작 제한은
  모두 없앴다. 사람들끼리 협의해서 쓰기로 했다.
  - **단주기/장주기 구분 자체는 그대로 유지한다.** 예약 버튼, 타임라인 그룹,
    화면 표시에서 계속 구분되며, 다만 두 분류에 적용되는 시간 규칙이 같을 뿐이다.
- **중복 금지:** 같은 GPU에서 시간이 겹치는 예약은 서버가 거부한다.
  - 겹침 조건: `새 시작 < 기존 종료 AND 새 종료 > 기존 시작`
  - 끝나는 시각과 시작 시각이 같은 경우(예: 10시 종료, 10시 시작)는 허용한다.
  - 두 사람이 동시에 신청해도 중복 저장되지 않도록 트랜잭션으로 처리한다.
- 검사는 반드시 서버(백엔드)에서 한다. 프론트엔드 검사는 편의용일 뿐이다.
- 관리자도 위 규칙을 똑같이 적용받는다. '관리자만 무시할 수 있는 규칙'은 없다.
  - 다만 **이미 시작된 예약의 시작 시각을 그대로 두고 종료 시각만 바꾸는 수정**은 허용한다.
    사용 중인 예약을 연장(또는 단축)하려면 시작 시각이 과거일 수밖에 없기 때문이다.
    이때 새 종료 시각은 지난 시각이면 안 된다. 시작 시각 자체를 과거로 옮기는 것은 여전히 거부한다.

## 6. 사용자와 권한
- 회원가입: 이름, 이메일, 비밀번호, 연구실 가입 코드(설정값). 가입 코드가 틀리면 가입 불가.
- 로그인: 이메일 + 비밀번호. 비밀번호는 반드시 해시(bcrypt 또는 argon2)로 저장한다.
- 일반 사용자: 본인 예약만 생성·취소·조기 종료할 수 있다.
- 관리자: 모든 예약을 수정·삭제할 수 있다. 첫 관리자 계정은 명령줄 스크립트로 생성한다.

## 7. 예약 상태와 색상
| 상태 | 의미 | 색상 |
|---|---|---|
| 예약 가능 | 해당 시간에 예약 없음 | 초록 |
| 예약됨 | 미래 시간에 예약이 잡혀 있음 | 빨강 |
| 사용 중 | 현재 시각이 예약 시간 안에 있음 | 노랑 |

- 이미 지나간 시간은 흐리게(회색 계열) 표시한다.
- 조기 종료: 사용 중인 예약의 종료 시각을 현재 시각(정시 올림)으로 앞당겨 남은 시간을 반납한다.
- 취소: 시작 전인 예약은 삭제할 수 있다.
- 연장: 본인은 연장할 수 없고, **관리자가** 사용 중인 예약의 종료 시각을 뒤로 미뤄 준다(5장 참고).

## 8. 화면 구성
1. **로그인 / 회원가입 화면**
2. **메인 화면 (예약 현황 타임라인)**
   - 가로축: 보고 있는 기간의 0시부터 14일(설정값), 시간 단위 칸
   - **이전 2주 / 오늘로 / 다음 2주** 버튼으로 기간을 옮겨 더 먼 미래도 볼 수 있다.
     지금 보고 있는 기간을 화면에 글로 표시하고, 오늘이 아닌 기간이면 그 사실을 알린다.
   - 세로축: GPU 12행, "단주기 6개 / 장주기 6개" 두 그룹으로 나누고 각 행에 서버 번호·GPU 번호·모델명 표시
   - 예약 블록에 예약자 이름 표시, 마우스를 올리거나(PC) 탭하면(휴대폰) 예약자·시작·종료 시각 표시
   - 현재 시각을 세로선으로 표시
   - 휴대폰에서도 볼 수 있도록 표는 가로 스크롤, 화면 전체는 가로로 넘치지 않게 한다.
3. **예약 신청**
   - "단주기 GPU 예약" 버튼 6개, "장주기 GPU 예약" 버튼 6개를 구분해서 배치
   - 버튼 클릭 → 해당 GPU의 시작 날짜·시간, 종료 날짜·시간 선택 → 신청
   - 해당 GPU에서 이미 예약된 시간대를 선택 화면에서 함께 보여준다.
     기간 제한이 없으므로 앞으로의 예약을 기간 제한 없이 모두 보여준다.
   - 규칙 위반 또는 중복 시 이유를 한국어로 명확히 표시한다.
4. **내 예약 목록**: 취소, 조기 종료 버튼
5. **관리자 화면**
   - 전체 예약 목록, 수정·삭제
   - **가입자 목록**: 이름, 이메일, 관리자 여부, 가입일, 현재·예정 예약 개수.
     보기 전용이며 계정을 고치거나 지우는 기능은 두지 않는다.
     관리자만 볼 수 있고 일반 사용자는 API로도 접근할 수 없다(403).
     비밀번호 해시는 어떤 경우에도 응답에 포함하지 않는다.

## 9. 구글 캘린더 연동 (마지막 단계에서 구현)
- 연구실 공용 구글 계정 1개로 **OAuth 로그인을 한 번** 하고, 발급된 토큰을 서버에 저장해 사용한다.
- **서비스 계정 방식은 사용하지 않는다.** (일반 Gmail 환경에서 참석자 초대가 안 됨)
- 예약 확정 시: 공용 캘린더에 일정 생성, 예약자 이메일을 참석자로 초대(초대 메일 발송)
  - 일정 제목 예: `[서버1 GPU2 장주기] 홍길동`
- 예약 취소 시 일정 삭제, 조기 종료·수정 시 일정 시간 수정
- 구글 일정 ID를 DB 예약 레코드에 저장한다.
- 구글 API 호출이 실패해도 예약 자체는 유지하고, 오류를 로그에 남기며 나중에 재시도할 수 있게 한다.
- 구글 연동은 설정으로 켜고 끌 수 있어야 한다. (꺼져 있어도 서비스는 정상 동작)
- `credentials.json`, 토큰 파일 등 비밀 정보는 git에 커밋하지 않는다 (.gitignore).

## 10. 배포와 운영
- `systemd` 서비스 파일 제공 (자동 시작, 비정상 종료 시 재시작)
- 방화벽(ufw) 포트 9080 허용 방법 안내
- SQLite DB 파일을 매일 백업하는 cron 설정 제공
- README.md에 설치, 실행, 재시작, 로그 확인, 백업 복구 방법을 초보자 기준으로 정리

## 11. 구현 단계
각 단계는 완료 조건을 모두 만족해야 끝난 것으로 본다.

- **Phase 1: 백엔드 기초**
  FastAPI 프로젝트, SQLite DB, GPU 초기 등록, 회원가입/로그인 API, 예약 생성/조회/취소 API, 예약 규칙과 중복 검사.
  완료 조건: 겹치는 예약 거부, 정시·순서·지난 시각 검사, 가입 코드 검사에 대한 자동 테스트(pytest)가 모두 통과.
- **Phase 2: 프론트엔드 기초**
  Vue 프로젝트, 로그인/회원가입 화면, 단주기·장주기 예약 버튼 12개, 날짜·시간 선택 예약 신청.
  완료 조건: 브라우저에서 가입 → 로그인 → 예약 신청 → 중복 신청 시 오류 메시지 확인.
- **Phase 3: 예약 현황 타임라인**
  8장의 메인 화면 타임라인, 상태별 색상, 현재 시각 표시, 기간 이동 버튼, 휴대폰 화면 대응.
  완료 조건: 예약들이 올바른 GPU 행·시간 칸에 올바른 색으로 표시됨.
- **Phase 4: 예약 관리와 관리자**
  내 예약 목록, 취소, 조기 종료, 관리자 화면(전체 예약 + 가입자 목록), 관리자 계정 생성 스크립트.
  완료 조건: 일반 사용자가 남의 예약을 취소할 수 없고 가입자 목록도 볼 수 없음을 테스트로 확인.
- **Phase 5: 배포**
  Vue 빌드 결과를 FastAPI가 서빙, systemd 서비스, 백업 cron, README.
  완료 조건: 데스크탑 재부팅 후 휴대폰에서 접속·예약 가능.
- **Phase 6: 구글 캘린더 연동**
  9장의 내용 구현.
  완료 조건: 예약 시 참석자에게 초대 메일 도착, 취소 시 일정 삭제.

## 12. 이번 범위에서 제외 (나중에 확장)
- 각 GPU 서버의 `nvidia-smi`를 읽어 실제 사용 여부를 자동 표시하는 기능
- 외부 인터넷(학교 밖)에서의 접속
